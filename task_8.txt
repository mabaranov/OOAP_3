Field
        Constructor(width, height) - создает игровое поле заданного размера
    Запросы:
        IsCellEmpty() - проверить что ячейка пустая
        GetObj(x, y) - получить объект по координатам
    Команды:
        Append(obj, x, y) - поместить объект на поле по заданным координатам
        Remove(obj) - удалить объект с поля
        Move(obj, x, y) - переместить объект на новые координаты

Level
        Constructor() - создать уровень: поле, еда, змея, стены
    Запросы:        
    Команды:
        MoveFood(x, y) - переместить еду (старая съедена, новая появилась) 
        MoveSnake() - переместить змею


Object
    - абстрактный класс объекта на поле

Snake: Object

Food: Object

Wall: Object

